[windows]
repository = "mingw64"
architecture = "x86_64"
prefix = "mingw64"
mirror_base_url = "https://mirror.msys2.org/mingw/mingw64"
lock_file = "config/windows-packages.lock.toml"
download_dir = "downloads/windows"
extract_dir = "_build/windows-root"
platform_tag = "win_amd64"

# Pacman resolves the complete transitive runtime closure. Keeping the major
# rendering components explicit also documents the intended Windows stack.
packages = [
    "mingw-w64-x86_64-pango",
    "mingw-w64-x86_64-cairo",
    "mingw-w64-x86_64-fontconfig",
    "mingw-w64-x86_64-freetype",
    "mingw-w64-x86_64-fribidi",
    "mingw-w64-x86_64-glib2",
    "mingw-w64-x86_64-harfbuzz",
]
